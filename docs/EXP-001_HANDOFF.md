# EXP-001 Data Handoff

The next required artifact is an authorized historical SPX/VIX/SKEW dataset. Place it into the strict template only after preserving the original source and acquisition metadata. The repository will validate provenance, point-in-time availability, timestamps, uniqueness, and immutable dataset identity before empirical execution.

Do not substitute synthetic or unverified data for the empirical dataset.
