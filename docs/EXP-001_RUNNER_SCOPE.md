# EXP-001 Runner Scope

This runner accepts only authorized historical CSV input, validates provenance and point-in-time integrity, fingerprints the immutable dataset, constructs locked observations, and executes the pre-registered experiment. It has no network, brokerage, credential, order, or portfolio capability.
