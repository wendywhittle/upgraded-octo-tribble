# EXP-001 Next Step

The software boundary is ready for an authorized historical dataset. No empirical claim should be made until such a dataset is supplied.

The next execution should use the existing authorized CSV endpoint and preserve its dataset manifest, point-in-time validation, source identity, methodology version, and immutable content hash.

If the dataset fails class-balance or provenance validation, the system should stop and report the reason rather than manufacture a result.
