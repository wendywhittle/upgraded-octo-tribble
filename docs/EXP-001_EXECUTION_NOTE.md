# EXP-001 Execution Note

The empirical execution boundary is authorized CSV input only. The runner validates provenance and point-in-time constraints, creates an immutable dataset identity, constructs observations without look-ahead, and invokes the locked Experiment #001 evaluator. No vendor scraping, credentials, brokerage connectivity, order placement, or portfolio mutation is permitted.
