# Registered Perspectives

AletheiaTelos now routes the simulation endpoint through the formal Computational Kaleidoscope registry. The registry contains ten research perspectives and enforces a non-executable capability profile.

The route intentionally returns `NO_DATA` when no verified evidence has been supplied. This keeps synthetic scaffolding from being mistaken for investment evidence while preserving the existing independent Monte Carlo, Skeptic, Observer, memory, and governance boundaries.
