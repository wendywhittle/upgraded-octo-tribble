# Agent Registry Integration

The simulation path now uses the formal agent registry. The default Computational Kaleidoscope roster is ten research perspectives. The registry rejects execution, brokerage, and portfolio mutation capabilities.

When no verified evidence is supplied, agents return `NO_DATA`; the system does not preserve synthetic demo claims as decision evidence.
