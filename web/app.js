const form=document.querySelector("#deal-form"),message=document.querySelector("#submit-message");
form.addEventListener("submit",async e=>{e.preventDefault();message.textContent="Submitting…";const data=Object.fromEntries(new FormData(form));for(const k of Object.keys(data)){if(data[k]==="")data[k]=null;else if(["purchase_price","noi","egi","operating_expenses","occupancy","ltv","interest_rate","amortization_years","hold_years","exit_cap_rate","closing_costs"].includes(k))data[k]=Number(data[k])}const r=await fetch("/api/deals",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok){message.textContent="We couldn't submit the deal. Please check the information and try again.";return}form.reset();message.textContent="Deal received. Your submission has been sent for internal review.";});
