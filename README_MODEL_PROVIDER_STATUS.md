# Model Provider Status

The model provider layer is an adapter boundary only. No vendor SDK or secret is embedded in the repository. A real provider can be added later without changing the evidence, risk, skeptic, governance, or execution boundaries.
